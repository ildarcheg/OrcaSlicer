// Catch2WithMain provides main(); this file only exists so the test target has at least one source.
// Note: OrcaSlicer vendors Catch2 v3, so the umbrella header is catch_all.hpp (not v2's catch.hpp).
#include <catch2/catch_all.hpp>
