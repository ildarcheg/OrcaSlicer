// project_ops.cpp -- mutations (add object, transforms, plate ops, ...)
// land in later phases (P2+). This translation unit exists in P1 so that the
// CMake source list is stable from the start of the project.
#include "project_ops.hpp"
